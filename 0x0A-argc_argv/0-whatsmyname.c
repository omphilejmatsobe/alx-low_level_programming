#include "stdio.h"
#include "stdlib.h"

/**
 * main - program that prints that prints its name.
 *
 * Return: 0 if success.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	exit(EXIT_SUCCESS);
}
